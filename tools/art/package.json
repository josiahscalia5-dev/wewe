{
  "name": "blast-collect-art",
  "private": true,
  "description": "Renders the interim art layers in /art from 3D/vector sources (placeholders until production art is supplied).",
  "type": "module",
  "scripts": {
    "build": "node render.mjs && python3 trim.py"
  },
  "dependencies": {
    "playwright-core": "1.56.1",
    "three": "0.186.1"
  }
}
